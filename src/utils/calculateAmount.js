// export const AMOUNT_PER_HOUR = 1000; // LKR per 60 minutes

// export function calcAmountFromDuration(durationMin = 60) {
//   // price = durationMin / 60 * AMOUNT_PER_HOUR
//   // Ensure numeric and round to nearest integer
//   const price = (durationMin / 60) * AMOUNT_PER_HOUR;
//   return Math.round(price);
// }
